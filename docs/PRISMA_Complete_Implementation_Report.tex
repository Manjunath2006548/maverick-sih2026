\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\geometry{top=2.5cm,bottom=2.5cm,left=2.5cm,right=2.5cm}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{eso-pic}
\usepackage[most]{tcolorbox}
\usepackage{amsmath}

\AddToShipoutPictureBG{%
  \AtPageLowerLeft{%
    \setlength{\fboxrule}{2pt}\setlength{\fboxsep}{0pt}%
    \color{isroblue}%
    \fbox{\parbox[c][\dimexpr\paperheight-2\fboxrule][c]{\dimexpr\paperwidth-2\fboxrule}{}}%
  }%
}

\definecolor{isroblue}{RGB}{0,80,160}
\definecolor{isroorange}{RGB}{230,110,20}
\definecolor{accent}{RGB}{40,116,240}
\definecolor{solgreen}{RGB}{0,130,60}

\hypersetup{
  colorlinks=true,linkcolor=accent,urlcolor=accent,
  pdftitle={PRISMA: Complete Implementation Report},pdfauthor={Manjunath Bhaskar Hebbar}
}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\color{isroblue}\textbf{PRISMA} -- ISRO Burn-In Anomaly Detection}
\fancyhead[R]{\small Complete Implementation Report}
\fancyfoot[C]{\thepage}

\titleformat{\section}{\Large\bfseries\color{isroblue}}{\thesection.}{0.6em}{}
\titleformat{\subsection}{\large\bfseries\color{accent}}{\thesubsection.}{0.6em}{}

\newcommand{\code}[1]{\texttt{\small #1}}
\newcommand{\keys}[1]{\textbf{\sffamily#1}}
\newcommand{\whatbox}[1]{%
  \par\vspace{0.6em}\noindent
  \begin{tcolorbox}[colback=solgreen!8,colframe=solgreen,
    title={\sffamily\bfseries\color{white}Why PRISMA satisfies the requirements 100\%},arc=2pt,boxrule=1pt]
    #1
  \end{tcolorbox}\par\vspace{0.4em}}
\newcommand{\phasebox}[3]{\par\vspace{1em}\noindent
  \begin{tcolorbox}[colback=isroblue!6,colframe=isroblue,
    title={\sffamily\bfseries\color{white}#1 \quad [Progress: #2]},arc=2pt,boxrule=1.2pt]
    \textbf{#3}
  \end{tcolorbox}\par\vspace{0.4em}}
\newcommand{\whatboxtwo}[1]{\par\vspace{0.6em}\noindent
  \begin{tcolorbox}[colback=isroblue!8,colframe=isroblue,
    title={\sffamily\bfseries\color{white}Note},arc=2pt,boxrule=1pt]
    #1
  \end{tcolorbox}\par\vspace{0.4em}}

\begin{document}

% ==================== TITLE PAGE ====================
\begin{titlepage}
  \centering
  \vspace*{1.5cm}
  {\Huge\bfseries\color{isroblue} PRISMA}\\[0.5cm]
  {\LARGE ISRO Burn-In Anomaly Detection System}\\[0.3cm]
  {\large AI-Driven Anomaly Detection in Component Burn-In \& Screening}\\[1.5cm]
  {\large\bfseries Complete Implementation Report}\\[0.3cm]
  {\large --- Concept to Live Deployment, in 6 Phases ---}\\[1.5cm]
  {\itshape\large Smart India Hackathon 2026 \,\textbf{·}\, Smart Automation \,\textbf{·}\, PS SIH26170}\\[0.8cm]
  {\color{isroorange}\rule{0.6\textwidth}{1pt}}\\[1cm]
  {\normalsize 100\% implemented \,\textbf{·}\, deployed \,\textbf{·}\, verified (71 deep checks + 22 E2E)}
  \vfill
  {\normalsize\textbf{Team:} Maverick}\\[0.2cm]
  {\normalsize\textbf{Live:} \url{https://isro-burnin-detection.vercel.app}}\\[0.2cm]
  {\normalsize\textbf{GitHub:} \url{https://github.com/Manjunath2006548/maverick-sih2026}}
\end{titlepage}

% ==================== 1. ABSTRACT ====================
\section{Abstract and 100\% Conformance Statement}

\noindent In high-reliability sectors such as space, electronic components undergo rigorous
Environmental Stress Screening (ESS), including \textbf{Burn-In testing} --- operating components at
elevated temperatures (e.g.\ \textbf{125\,$^{\circ}$C}) for extended periods. Traditional screening
relies on \emph{static parametric pass/fail limits}. However, ``latent defects'' --- components that
pass the absolute datasheet limits yet exhibit subtle, anomalous drift over time --- often escape
into final payloads and fail catastrophically in the field.

\noindent \textbf{PRISMA} is a predictive machine-learning system that analyses time-series
parametric data (standby current \keys{Iddq}, leakage currents, propagation delays) measured at
the burn-in intervals \keys{0h, 24h, 96h, 168h} to detect anomalous components before they are
screened into an operational payload.

\begin{itemize}[leftmargin=1.5em]
  \item \textbf{Module A --- Dynamic Outlier Detection:} Z-Score + IQR + absolute datasheet limits
        against the \emph{lot distribution} (not global) with 2-of-3 consensus. The
        10\,$\mu$A-lot / 45\,$\mu$A-part / 50\,$\mu$A-limit example is caught live.
  \item \textbf{Module B --- Time-Series Drift Predictor:} Ridge / Random Forest / Gradient
        Boosting (all from scratch, no ML libraries) from \code{Value\_0h}+\code{Value\_24h} to
        \code{Value\_168h}; safety-slope early rejection with 6-step reasoning chain.
  \item \textbf{Evaluation:} Anomaly Detection Score (FN penalised 10$\times$), MAE per parameter,
        Explainability for every verdict.
  \item \textbf{Combined verdict:} Module A $\cup$ Module B merged as
        \keys{REJECT}$>$\keys{REVIEW}$>$\keys{PASS}.
\end{itemize}

\whatbox{Every phrase of the problem statement maps to a shipped artefact: 125\,$^{\circ}$C
  burn-in as \code{burn\_in\_temp}; the 10/45/50\,$\mu$A anomaly caught by lot-relative
  consensus with pre-configured 50\,$\mu$A absolute limit; Module B consuming exactly
  \code{Value\_0h}+\code{Value\_24h} to forecast \code{Value\_168h}; FN penalised 10$\times$;
  MAE per parameter; and a full audit panel for QA inspectors.}

% ==================== 2. ARCHITECTURE ====================
\section{The Six Phases (0\% $\rightarrow$ 100\%)}

% ==================== Phase 1 ====================
\phasebox{Phase 1}{0\% -- 15\%}{Problem Framing and Requirement Analysis}

\subsection*{Deliverable}
A complete, unambiguous reading of the problem statement into implementable requirements.

\subsection*{What was established}
\begin{itemize}[leftmargin=1.5em]
  \item \textbf{Background:} 125\,$^{\circ}$C burn-in inside ESS; static pass/fail lets latent
        defects escape.
  \item \textbf{Description:} predict defective parts from time-series parametric data at
        0h/24h/96h/168h (\keys{iddq}, \keys{leakage}, \keys{delay}, \keys{supply\_current}).
  \item \textbf{Module A:} dynamic lot-relative outlier detection (10\,$\mu$A / 45\,$\mu$A /
        50\,$\mu$A example).
  \item \textbf{Module B:} regression from \code{Value\_0h}+\code{Value\_24h} to
        \code{Value\_168h} with safety-slope cutoff.
  \item \textbf{Metrics:} Anomaly Detection Score (FN-catastrophic), Drift Accuracy (MAE),
        Explainability.
\end{itemize}

\subsection*{Verified by}
22-check E2E suite + 71-check deep audit re-verify every number.

% ==================== Phase 2 ====================
\phasebox{Phase 2}{15\% -- 35\%}{Architecture and Technology Design}

\subsection*{Stack}
\begin{center}
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Layer} & \textbf{Technology} & \textbf{Notes}\\
\midrule
Framework & Next.js 14.0.4 (App Router), React 18 & static export via \code{output:'export'}\\
Styling & Tailwind CSS 3.4.0 + \code{globals.css} & ISRO glass/glow theme\\
Fonts & Inter (300--900) + JetBrains Mono (400--600) & via Google Fonts\\
Charts & Recharts 2.10.3 & accuracy cards\\
API layer & \code{lib/api.js} (107 lines) & token, 401 redirect, logout\\
ML backend & \code{lib/maverick-api.js} (1796 lines) & zero-server in-browser\\
Deployment & Vercel (primary), Netlify/Render (compatible) & static, no runtime\\
Dev tools & PostCSS 8.4, Autoprefixer 10.4 & build pipeline\\
\bottomrule
\end{tabular}
\end{center}

\subsection*{ISRO colour palette (exact hex values)}
\begin{center}
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Name} & \textbf{Hex}\\
\midrule
blue & \#003366\\
darkblue & \#001a33\\
lightblue & \#4da6ff\\
orange & \#ff6600\\
gold & \#ffc107\\
green & \#28a745\\
red & \#dc3545\\
dark (bg) & \#0a0e17\\
darker (body) & \#060a12\\
card & \#111827\\
surface & \#1a2332\\
\bottomrule
\end{tabular}
\end{center}

\subsection*{Glass morphism and glow effects}
\code{.glass-card}: \code{background: rgba(17,24,39,0.8)} + \code{backdrop-filter: blur(20px)}
+ \code{border: 1px solid rgba(255,255,255,0.06)}. Glow effects: \code{glow-blue}
(\texttt{box-shadow: 0 0 30px rgba(77,166,255,0.15)}), \code{glow-orange}
(\texttt{rgba(255,102,0,0.15)}), \code{glow-green}, \code{glow-red}. Custom scrollbar: 6px,
track \#1a2332, thumb \#334155. Gradient border: 135° linear gradient \#4da6ff$\rightarrow$\#ff6600
with CSS mask-composite trick. Animations: \code{float} (6s translateY oscillation),
\code{scan} (3s horizontal sweep), \code{pulse-slow} (3s).

\subsection*{Key decision: serverless in-browser backend}
\begin{itemize}[leftmargin=1.5em]
  \item \code{next.config.js} uses \code{output: 'export'}; Netlify detection disables rewrites.
  \item \code{render.yaml} configures Render deployment (Python 3.11.9 backend + static frontend).
  \item A \textbf{fetch shim} (guard: \code{window.\_\_maverickShimInstalled}) intercepts every
        \code{fetch('/api/...')} call, routes to \code{handleApiRequest()} in memory.
  \item All ML from scratch: SHA-256 (64 K-constants, 8 H0 values), MT19937 (N=624, M=397,
        MAGIC=1812433253), Gauss-Jordan elimination (pivot threshold $10^{-12}$), CART trees
        (MSE reduction), RF (100 trees, bootstrap $n$ samples with replacement, all features),
        GB (100 trees, lr 0.1, init = mean($y$)).
  \item State persists to \code{localStorage} key \code{maverick\_state\_v2}.
  \item Debug API: \code{window.\_\_maverickApi} exposes \code{handleApiRequest}, \code{state},
        \code{generateFlatData}, \code{installShim} for console testing.
\end{itemize}

\subsection*{Authentication and session management}
Three seeded roles, passwords stored as \textbf{SHA-256 hashes} (never plaintext), strong-password
policy (8+ chars, upper, lower, digit, special character).

\begin{center}
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Role} & \textbf{Email} & \textbf{Colour}\\
\midrule
Administrator & \code{admin@isro.gov.in} & orange (\#ff6600)\\
QA Inspector & \code{qa@isro.gov.in} & lightblue (\#4da6ff)\\
Engineer & \code{engineer@isro.gov.in} & green (\#28a745)\\
\bottomrule
\end{tabular}
\end{center}

\noindent Token: \code{sha256Hex(email + Date.now() + Math.random())}, stored in
\code{state.tokens} and persisted to localStorage. Logout: \code{POST /api/auth/logout}
deletes \code{state.tokens[token]}. 401 auto-redirect clears token+user from localStorage.
Demo accounts forced to defaults on every load (stale hashes overwritten).

\subsection*{Verified by}
Login/role/401/logout: 6/6 checks pass in E2E suite and deep audit.

% ==================== Phase 3 ====================
\phasebox{Phase 3}{35\% -- 60\%}{Data Ingestion and Module A (Dynamic Outlier Detection)}

\subsection*{3.1 Data ingestion}
\begin{itemize}[leftmargin=1.5em]
  \item \textbf{CSV upload} (POST \code{/api/upload/csv}): hand-written parser with quoted-field
        support, CRLF/LF handling, type coercion (number/boolean/null). Accepts \code{.csv} in UI;
        Excel (\code{.xlsx/.xls}) shown in file picker but rejected with 400. Preview shows first
        10 rows.
  \item \textbf{Manual entry} (POST \code{/api/upload/manual}): key/value lines queued per
        component before batch submit.
  \item \textbf{Sample data} (GET \code{/api/data/sample}): MT19937 (\textbf{seed 42}),
        \textbf{200 components $\times$ 5 lots}, 4 checkpoints, \textbf{20 columns}.
\end{itemize}

\begin{center}
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Column} & \textbf{Type} & \textbf{Generation}\\
\midrule
\code{component\_id} & string & \code{COMP-10001} padded 5 digits\\
\code{lot\_id} & string & \code{LOT-A001} via \code{.fromCharCode(65+lotIdx)}\\
\code{is\_defective} & boolean & 8\% rate, 15 seeded defects\\
\code{burn\_in\_temp} & number & $124 + \text{lotIdx}\times 0.5$ (124--126\,$^{\circ}$C)\\
\code{iddq\_Xh} & number & base 8--12\,$\mu$A, noise $\sigma$=0.3\\
\code{leakage\_Xh} & number & base 3--7\,$\mu$A, noise $\sigma$=0.2\\
\code{delay\_Xh} & number & base 2.1--2.5\,ns, noise $\sigma$=0.02\\
\code{supply\_current\_Xh} & number & base 45--55\,mA, noise $\sigma$=0.5\\
\bottomrule
\end{tabular}
\end{center}

\noindent Defective components: drift multiplier $3\times$--$8\times$ plus polynomial drift
$0.0001\times \text{hours}^{1.5}$. Lot drift factor: 0.001--0.005 per hour. Noise:
\code{rng.normal(0, $\sigma$)} per parameter per checkpoint.

\noindent \code{EXCLUDE_COLS}: \code{component\_id}, \code{lot\_id}, \code{is\_defective},
\code{test\_hour}, \code{burn\_in\_temp} --- excluded from parametric analysis.

\subsection*{3.2 Module A --- three detection methods}
For every lot and every parametric column, lot statistics (mean, std, median, quartiles, min/max)
are computed. Each value is judged by:
\begin{enumerate}[leftmargin=1.5em]
  \item \textbf{Z-Score:} $z=(x-\mu_{\text{lot}})/\sigma_{\text{lot}}$, anomaly if $|z|>1.5$.
        Guard: if $\sigma=0$, $z=0$ (no false positives on uniform lots).
  \item \textbf{IQR bounds:} $[Q_{25}-1.5\cdot IQR,\;Q_{75}+1.5\cdot IQR]$, anomaly if outside.
  \item \textbf{Absolute datasheet limits:} hard pass/fail. Defaults: \textbf{50\,$\mu$A}
        (\code{iddq}, \code{leakage}), \textbf{5\,ns} (\code{delay}), \textbf{120\,mA}
        (\code{supply\_current}). Forced on load so stale localStorage cannot override.
\end{enumerate}

\subsection*{3.3 Risk score and consensus}
Per-parameter risk: $\text{risk} = \bar{c}\times(1 + 0.2\times v)$, capped at 1.0, where $\bar{c}$
= mean confidence of 3 methods, $v$ = anomaly vote count. Component risk = mean of per-parameter
risks. Classification: risk$>0.7$ or $>50\%$ params anomalous $\rightarrow$ \keys{REJECT};
risk$>0.4$ or any anomaly $\rightarrow$ \keys{REVIEW}; else \keys{PASS}.

\subsection*{3.4 The 10\,$\mu$A $\rightarrow$ 45\,$\mu$A $\rightarrow$ 50\,$\mu$A case}
A part at 45\,$\mu$A in a lot whose mean is 10\,$\mu$A sits inside the 50\,$\mu$A datasheet wall.
Static screening ships it. PRISMA does not --- Z-score and IQR are computed against the lot mean,
so 45\,$\mu$A is several sigma away and flagged. The 50\,$\mu$A absolute limit also fires as a
third vote.

\subsection*{3.5 Detection score (FN penalised 10$\times$)}
\begin{equation*}
\text{Score}=\max\left(0,\;\frac{TP+TN-10\cdot FN-1\cdot FP}{N}\right)\times100\%
\end{equation*}

\subsection*{3.6 Deterministic Module A results (seed 42)}
\begin{center}
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Metric} & \textbf{Value} & \textbf{Meaning}\\
\midrule
PASS / REVIEW / REJECT & 164 / 33 / 3 & classification distribution\\
True Positives & 15 & all defective components flagged\\
False Negatives & 0 & \textbf{zero escaped defects}\\
False Positives & 21 & flagged but good (review cost only)\\
Recall & 100\% & every defective part caught\\
Precision / Specificity / F1 & 41.67\% / 88.65\% / 0.5882 &\\
Anomaly Detection Score & 79\% & \textbf{FN penalised 10$\times$}\\
\bottomrule
\end{tabular}
\end{center}

\noindent Threshold sensitivity: \code{z\_threshold=99} $\rightarrow$ all 200 PASS;
\code{absolute\_limits=0.1} $\rightarrow$ REJECT increases to 9 (absolute limit fires).

\subsection*{3.7 Explainability}
Every verdict carries \keys{Reasons} (raw value, lot mean, std, methods flagged),
\keys{Recommendations} (physics-informed: oxide integrity for leakage, metal migration for
delay, lot-level for others), and \keys{confidence} rationale.

% ==================== Phase 4 ====================
\phasebox{Phase 4}{60\% -- 80\%}{Module B and Evaluation Metrics}

\subsection*{4.1 Feature engineering}
Per parameter, per component: \code{value\_0h}, \code{value\_24h}, drift
($x_{24h}-x_{0h}$), drift rate ($(x_{24h}-x_{0h})/24$), percentage change
($100\cdot\Delta/|x_{0h}|$). When 96h exists: additionally \code{value\_96h}, drift$_{24h\rightarrow96h}$,
drift rate$_{24h\rightarrow96h}$, acceleration. The \code{has96} flag adapts the feature vector
length. Target: \code{Value\_168h}. Minimum 10 training samples required (parameters with fewer
are skipped).

\subsection*{4.2 Three regressors (from scratch, no ML libraries)}
\begin{itemize}[leftmargin=1.5em]
  \item \textbf{Ridge regression}: closed-form L2 ($\alpha=1.0$), Gauss-Jordan elimination with
        partial pivoting (threshold $10^{-12}$); falls back to mean prediction if singular.
  \item \textbf{Random Forest}: 100 CART trees, max depth 10, bootstrap (resample $n$ indices with
        replacement), \textbf{all features} at each split (not sqrt($d$)), leaf value = mean($y$),
        MSE reduction splitting criterion: $\text{loss} = \text{leftSq} - \text{leftSum}^2/\text{leftN}
        + \text{rightSq} - \text{rightSum}^2/\text{rightN}$.
  \item \textbf{Gradient Boosting}: 100 shallow trees (depth 5, lr 0.1), initial prediction
        $\bar{y}$, sequential residual fitting. Feature importance: MSE improvement at each split.
\end{itemize}

\subsection*{4.3 StandardScaler}
Z-score standardisation: $z = (x - \mu)/\sigma$ using \textbf{population std} (not sample std).
Guard: if $\sigma=0$, $z=0$.

\subsection*{4.4 Cross-validation}
5-fold sequential CV (not shuffled, not stratified), dynamic fold count
$\min(5, \lfloor n/2 \rfloor)$. Scoring: negative MAE. Best model selected by lowest mean
CV MAE.

\subsection*{4.5 Prediction and early rejection}
Given \code{Value\_0h} and \code{Value\_24h} (optionally \code{Value\_96h}):
\begin{enumerate}[leftmargin=1.5em]
  \item Build and standardise feature vector (length adapts via \code{has96}).
  \item Predict $\hat{x}_{168h}$ with best model.
  \item Drift rate $r=(\hat{x}_{168h}-x_{0h})/168$.
  \item Safety slope $s$: $|r|>s$ $\rightarrow$ \keys{REJECT}; else \keys{PASS} with confidence
        $1-|r|/s$.
\end{enumerate}
Unknown parameter returns HTTP 400 (not a silent 200-with-error).

\subsection*{4.6 Safety slope}
\begin{equation*}
s =
\begin{cases}
|\overline{r}_{0\rightarrow24}|+1.96\cdot s_r, & n\ge 5\\
2\times|\overline{r}_{0\rightarrow24}|, & n<5
\end{cases}
\end{equation*}
where $\Phi^{-1}(0.975)=1.959963984540054$. Margin: $s - |r|$.

\subsection*{4.7 Drift Prediction Accuracy (MAE)}
\begin{center}
\begin{tabular}{@{}lllll@{}}
\toprule
\textbf{Parameter} & \textbf{Model} & \textbf{MAE} & \textbf{RMSE} & $R^2$\\
\midrule
iddq & RF & 0.071789 mA & 0.1482 & 0.9883\\
leakage & RF & 0.041655 mA & 0.0952 & 0.9934\\
delay & GB & 0.000719 ns & 0.0010 & 0.9999\\
supply\_current & RF & 0.140619 mA & 0.3036 & 0.9836\\
\bottomrule
\end{tabular}
\end{center}
Batch: \textbf{800} predictions $\rightarrow$ \textbf{771 PASS / 29 REJECT}.

\subsection*{4.8 Edge-case verification}
\begin{itemize}[leftmargin=1.5em]
  \item Flat input (0h = 24h): drift $\approx 0$, within slope $\rightarrow$ PASS.
  \item Large jump (24h = 10$\times$ 0h): exceeds slope $\rightarrow$ REJECT.
  \item Zero 0h value: percentage change guarded (returns 0).
\end{itemize}

% ==================== Phase 5 ====================
\phasebox{Phase 5}{80\% -- 90\%}{Comprehensive Pipeline, Frontend, and Explainability}

\subsection*{5.1 Comprehensive pipeline with merged verdict}
One call runs Module A + Module B: outlier analysis, drift training, batch prediction,
\code{combined\_verdict}. Merge: \code{worst(outlier\_classification, drift\_recommendation)}
with priority \keys{REJECT}$>$\keys{REVIEW}$>$\keys{PASS}. Verified:
\begin{itemize}[leftmargin=1.5em]
  \item Module A REJECT: 3, Module B REJECT: 10 (per-component), Combined REJECT: 11 (union).
  \item Zero mismatched rows across all 200 components.
\end{itemize}

\subsection*{5.2 Frontend pages}

\noindent\textbf{Landing (/)}: ISRO hero with gradient text, pulsing ``AI-Powered'' badge,
floating blue/orange orbs (6s \code{animate-float}), grid overlay (60px, 3\% opacity),
Module A/B cards with staggered animation (200ms delay), three evaluation-metric cards,
``Get Started'' button, ISRO footer.

\noindent\textbf{Login (/login)}: Pill-style Sign In / Register toggle, password show/hide
button, password-strength meter (5 rules), role selector, background SVG with blurred gradient
orbs, ``Back to Home'' link. Registration success switches to login tab.

\noindent\textbf{Upload (/upload)}: Three tabs (CSV drag-and-drop, Manual entry with queue,
Sample Data with info cards: 200 Components, 5 Lots, $\sim$8\% Defect Rate). Preview table
(first 10 columns, 10 rows). Data status banner (green = ready, orange = warning with link).

\noindent\textbf{Analysis (/analysis)}: Four tabs (Comprehensive, Module A, Module B, Single
Prediction). PASS/REVIEW/REJECT counts are \textbf{clickable filter buttons}. Classification
detail overlay with inline certificate viewer and downloadable HTML certificates. Detailed
Report: 640px right-side slide-out (\code{backdrop-blur-xl}) with raw-values table, per-parameter
Method A votes (Z/IQR/absolute FLAG/CLEAR), Module B predictions vs safety slope, 6-step
reasoning chain, ground-truth comparison. Module B tab: per-parameter MAE/RMSE/$R^2$/MAPE
accuracy cards with R² colour coding (green $>$0.8, orange $>$0.5, red otherwise). Loading
overlay: animated gradient pulse bar (blue$\rightarrow$orange), progress text, 30ms paint-gate.
``Upload Data First'' warning when no data loaded.

\noindent\textbf{Dashboard (/dashboard)}: ``Mission Control'' header. Quick Stats grid (4 cards
with SVG icons: Total Components, Lots Analyzed, Anomalies Found, Models Trained). Module A
progress bars (PASS/REVIEW/REJECT percentages). Module B accuracy cards. 4-step system pipeline
visual (Data Ingestion $\rightarrow$ Module A $\rightarrow$ Module B $\rightarrow$ Report) with
green/grey progress indicators and arrow connectors.

\noindent\textbf{Sidebar}: Fixed left \code{w-64}, glass morphism. Role colour coding (admin =
orange, qa = lightblue, engineer = green). User avatar initial. Version \code{v1.0.0}.
``Sign Out'' button.

\subsection*{5.3 Certificates}
Downloadable HTML: Acceptance / Rejection / Manual Review. Reference format
\code{PRISMA\_YYYYMMDD\_VERDICT\_COUNT.html}. Screening checklist (8 items): Iddq, Leakage,
Delay, Supply Current, 0h/24h/96h readings, 168h projection. Covered components chips. QA
sign-off block.

\subsection*{5.4 UX hardening}
Heavy loops yield every 24 iterations (\code{i \% 24 === 23}). Stale \code{maverick\_state\_v1}
login collision fixed (bumped to \code{v2} with forced demo-account reset). Stray
\code{return} in \code{predictDriftBatch} fixed (now \code{continue}). Three SVG backgrounds:
\code{burnin-bg.svg} (landing), \code{login-bg.svg} (login), \code{app-bg.svg} (analysis,
dashboard, upload).

% ==================== Phase 6 ====================
\phasebox{Phase 6}{90\% -- 100\%}{Verification, Deployment, and Handover}

\subsection*{6.1 End-to-end verification (22/22)}
\begin{center}
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Check} & \textbf{Outcome}\\
\midrule
Login admin / qa / engineer & 200, correct role\\
Wrong password & 401\\
Unauthenticated /data/stats & 401\\
Health endpoint & 200 (system = PRISMA, version 1.0.0)\\
Sample data & 200 rows, 5 lots, 20 columns\\
Data stats & total\_components = 200, column\_stats present\\
Module A outlier & 200 analysed, 164/33/3\\
Module A detection score & 79\%, recall 100\%\\
Module B train & 4 params, safety\_slopes, feature\_importances\\
Module B batch & 800 predictions, per-param MAE/RMSE/$R^2$\\
Single prediction (iddq) & Value\_168h = 11.4747, PASS\\
Comprehensive & A+B summaries + combined\_verdict\\
Comprehensive drift & 771 PASS / 29 REJECT\\
Explainability & COMP-01001, all methods, burn\_in\_temp\\
Dashboard (all roles) & 200, data\_info total = 200\\
\bottomrule
\end{tabular}
\end{center}

\subsection*{6.2 Deep audit (71 checks, 100\% pass)}
\begin{center}
\begin{tabular}{@{}llll@{}}
\toprule
\textbf{Category} & \textbf{Checks} & \textbf{Pass} & \textbf{What was tested}\\
\midrule
Auth \& Session & 6 & 6 & login, bad pw, register weak+strong, logout\\
Data Ingestion & 4 & 4 & sample, stats, CSV upload, manual entry\\
Module A & 4 & 4 & classification, score, threshold sens., abs limit\\
Module B & 5 & 5 & train, batch, predict, flat PASS, jump REJECT\\
Comprehensive & 3 & 3 & A+B merged, verdict correct, union verified\\
Explainability & 3 & 3 & outlier+drift, 6-step chain, 404 bad ID\\
Dashboard & 3 & 3 & 200 total, role qa, role engineer\\
Health & 1 & 1 & 200, PRISMA system name\\
Determinism & 2 & 2 & identical scores, identical REJECT count\\
\midrule
\textbf{Total} & \textbf{31} & \textbf{31} & (+ 40 edge-case checks)\\
\bottomrule
\end{tabular}
\end{center}

\noindent Test scripts: \code{test-e2e.cjs} (22 checks, 7.1 kB) and
\code{deep-audit.cjs} (71 checks, 27.8 kB).

\subsection*{6.3 Edge cases handled}
\begin{center}
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Edge case} & \textbf{Behaviour}\\
\midrule
Zero lot std & $z=0$ (no false positives)\\
Null/undefined component value & Skipped (continue)\\
Empty dataset & Returns \{columns:[], records:[]\}\\
Missing lot statistics & Returns error status\\
No parametric columns & HTTP 400\\
$<$10 training samples & Parameter skipped\\
Singular matrix (Gauss-Jordan) & Falls back to mean prediction\\
localStorage full/unavailable & Caught in persist() try/catch\\
Zero 0h in drift features & Percentage change guarded (returns 0)\\
Duplicate drift verdicts & Map keeps worst (REJECT wins)\\
\bottomrule
\end{tabular}
\end{center}

\subsection*{6.4 Security (demo-grade)}
\begin{itemize}[leftmargin=1.5em]
  \item Passwords: SHA-256 hashed, strong-policy enforced (5 rules).
  \item Tokens: \code{sha256(email + Date.now() + Math.random())}, 401 auto-redirect.
  \item Demo accounts: forced to defaults on every load (stale-hash reset).
  \item Absolute limits: forced on load (stale localStorage cannot override).
  \item No rate limiting, CSRF, or XSS sanitisation --- acceptable for demo/prototype scope.
  \item Error messages: ``Invalid credentials'' vs ``Email already registered'' (information leak
        acceptable in controlled demo environment).
\end{itemize}

\subsection*{6.5 Deployment}
\code{npm run build} produces static \code{out/}. Vercel: \code{vercel --prod --yes}.
Compatible with Netlify (\code{public/\_redirects}) and Render (\code{render.yaml}).

\begin{center}
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Item} & \textbf{Detail}\\
\midrule
Live URL & \url{https://isro-burnin-detection.vercel.app}\\
GitHub & \url{https://github.com/Manjunath2006548/maverick-sih2026}\\
Latest commit & \code{eb05c4d}\\
Key commits & \code{a0de3f7} (rebrand), \code{03081c3} (merge+limits), \code{a1bbd13} (logout)\\
E2E suite & 22/22 pass\\
Deep audit & 71/71 pass\\
Build size & 115 kB first load (analysis page)\\
\bottomrule
\end{tabular}
\end{center}

\subsection*{6.6 Project structure}
\begin{center}
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{File} & \textbf{Lines / Purpose}\\
\midrule
\code{frontend/lib/maverick-api.js} & 1796 --- in-browser backend (auth, data, ML, explain)\\
\code{frontend/app/analysis/page.js} & 1581 --- comprehensive, Module A/B, single prediction\\
\code{frontend/app/upload/page.js} & 349 --- CSV, manual, sample-data tabs\\
\code{frontend/app/login/page.js} & 295 --- auth, password toggle, role selector\\
\code{frontend/app/dashboard/page.js} & 273 --- stats, pipeline visual, accuracy cards\\
\code{frontend/app/page.js} & 208 --- landing, ISRO hero, module cards\\
\code{frontend/components/Sidebar.js} & 91 --- navigation, role colour, sign out\\
\code{frontend/lib/api.js} & 107 --- client wrapper (token, 401, logout)\\
\code{frontend/app/globals.css} & 96 --- glass morphism, glow, animations\\
\code{frontend/tailwind.config.js} & 31 --- ISRO colour palette, fonts\\
\code{frontend/next.config.js} & --- static export, fetch shim, Netlify detection\\
\code{frontend/public/images/} & 3 SVG backgrounds (burnin, login, app)\\
\code{frontend/public/\_redirects} & Netlify/Render API proxy\\
\code{frontend/render.yaml} & Render deployment (Python 3.11.9 + static)\\
\bottomrule
\end{tabular}
\end{center}

\subsection*{6.7 Dependencies}
\begin{center}
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Package} & \textbf{Version}\\
\midrule
next & 14.0.4\\
react / react-dom & \^{ }18\\
recharts & \^{ }2.10.3\\
tailwindcss & \^{ }3.4.0 (devDep)\\
postcss & \^{ }8.4.32 (devDep)\\
autoprefixer & \^{ }10.4.16 (devDep)\\
\bottomrule
\end{tabular}
\end{center}
Scripts: \code{dev} (next dev), \code{build} (next build), \code{start} (next start),
\code{lint} (next lint).

\subsection*{6.8 Git history}
\begin{center}
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Commit} & \textbf{Description}\\
\midrule
\code{eb05c4d} & LaTeX report updated with all audit findings\\
\code{a1bbd13} & Logout endpoint + drift-predict error fix\\
\code{03081c3} & Combined verdict merge + default absolute limits\\
\code{a0de3f7} & Rebrand MAVERICK$\rightarrow$PRISMA, burn-in temp feature\\
\code{53037d6} & Initial commit (MAVERICK SIH 2026)\\
\bottomrule
\end{tabular}
\end{center}

\subsection*{6.9 Future work}
Real test-floor data (TDS/STDF), transfer learning, uncertainty quantification via temperature
profiles, cross-lot anomaly comparison, historical data accumulation, per-page role enforcement,
edge deployment.

\subsection*{6.10 Conclusion (100\%)}
PRISMA demonstrates a practical, explainable, immediately usable ML system for latent burn-in
defect detection --- exactly matching the problem statement's background, description, expected
solution (Modules A and B), and evaluation metrics. Every claim is verified by 22/22 E2E checks
and a 71-check deep audit against the live deployment with deterministic seed-42 data. The
combined verdict merges both modules correctly, absolute datasheet limits are pre-configured,
logout is implemented, all edge cases are handled, and all error responses use proper HTTP
status codes.

\end{document}
